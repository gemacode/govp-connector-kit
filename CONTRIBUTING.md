# Contribuir

Abre una incidencia antes de cambiar el contrato público. Para preparar una
contribución ejecuta `npm ci && npm run check` y no incluyas tokens, datos de
clientes ni credenciales de GOVP Exchange. Las contribuciones se publican bajo
Apache-2.0.
